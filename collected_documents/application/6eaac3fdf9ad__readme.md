# Data Contracts

Data-contract reports describe the actual structure observed in
immutable bronze data.

They are generated before normalization logic is written.

A candidate type such as `integer-like` or `numeric-like` is an
observed storage pattern, not a semantic business definition.

The ODBus source is a historical entity baseline. Its fields must not
be interpreted as proof that a business is currently operating.
